export enum Keywords{
    "if" = "h-if",
    "else" = "h-else",
    "for" = "h-for",
    "in" = " in ",
    "app" = "app",
    "slot" = "h-slot",
    "nativeEvent" = "$event",
    "eventPrefix" = "@eventfor-",
}

