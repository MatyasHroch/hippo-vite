export enum Keywords{
    "if" = "data-h-if",
    "else" = "h-else",
    "for" = "data-h-for",
    "in" = " in ",
    "app" = "app",
    "slot" = "h-slot",
    "nativeEvent" = "$event",
    "eventPrefix" = "@eventfor-",
}

